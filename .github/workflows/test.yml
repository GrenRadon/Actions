name: Hola Mundo

on: [push]

jobs: 
    hola:
        runs-on: ubuntu-latest

        steps: 
        - name: Hola Mundo
          run:  echo "Hola mundo"
        - name: touch file
          run: touch hola.txt
        - name: Set text 
          run: 
    
    other:
        runs-on: ubuntu-latest

        steps: 
        - name: LS
          run:  ls -al
